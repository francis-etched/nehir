---
"nehir": patch

---

Keep portrait rows sized and reachable when focusing or scrolling. Park offscreen rows beside the display, and stage clipped windows before shrinking so side-by-side splits do not become overlapping tabs after a display-clamped resize. Keep portrait tab rails in the same order as side-by-side windows.
